User:
  columns:
    name: string(50)
    email: string(50)

Organizations:
  actAs:
    Timestampable: ~
  columns:
    title: string(255)
    body: clob
    view_count: integer
    recommend_level:
      type: enum
      values: [0,1,2]
      default: 2
    category_id: integer
  relations:
    User:
      local: category_id
      foreign: id
      foreignAlias: Organizations

Grants:
  columns:
    body: clob
    user_id: integer
    organization_id: integer
  relations:
    Organizations:
      local: organization_id
      foreign: id
      foreignAlias: Grants

